name: TaskVerse-Boilerplate CI/CD Pipeline

on:
  workflow_dispatch

jobs:
    deploy:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - name: Deploy
          uses: superfly/flyctl-actions@1.1
          with:
            args: deploy --app taskverse-boilerplate  
